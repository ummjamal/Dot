# بقالة الشعيبي — V4 Ultimate

## أهم ما تغيّر
- إصدار `4.0.0` (`versionCode 12`).
- تدفق دفع احترافي: نقدي أو تحويل، خصم، مبلغ مستلم، الباقي للعميل، وملاحظات.
- إلغاء الفاتورة بطريقة محاسبية آمنة مع إعادة الكمية إلى المخزون وتسجيل الحركة.
- سجل تدقيق محلي `audit_log` للعمليات الحساسة.
- فهارس SQLite جديدة لرفع سرعة البحث والسجلات مع نمو البيانات.
- إحصاءات اليوم لا تحتسب الفواتير الملغاة.
- قيمة المخزون بسعر التكلفة والبيع + إجمالي المبيعات والربح في الإعدادات.
- فلاتر مخزون: الكل / منخفض / نافد.
- فاتورة نصية أغنى للمشاركة: طريقة الدفع، الخصم، المستلم، الباقي، الملاحظات، والتذييل.
- تنبيه واضح إذا بقيت كلمة المرور الافتراضية `123456`.
- تذييل فاتورة قابل للتعديل من الإعدادات.
- تحسين ألوان الهوية، Toolbar متدرج، انتقالات أنعم بين الأقسام.
- صوت واهتزاز خفيف عند نجاح المسح والبيع، واهتزاز خطأ في الحالات الحرجة.
- إزالة subtitle من Toolbar لمنع التداخل الذي ظهر على بعض أجهزة Samsung.
- استمرار العمل Offline بدون Firebase أو اتصال إنترنت.

## قواعد البيانات
ترقية قاعدة البيانات من الإصدار 2 إلى 3 تتم تلقائياً بدون حذف البيانات.
تمت إضافة حقول إلى `sales`: الخصم، المدفوع، الباقي، الملاحظات، مع إنشاء جدول `audit_log` وفهارس أداء.

## ملاحظة مهمة
لا يتم وضع مفتاح توقيع Release داخل المشروع أو Git. يجب إنشاء المفتاح وحفظه بشكل خاص عند تجهيز نسخة Google Play.

## الجودة المستمرة
- أضيف Quality Gate على GitHub Actions لتشغيل الاختبارات وAndroid Lint وبناء APK لكل Pull Request والتغييرات المهمة.
- أضيف `scripts/verify_v4.sh` لإعادة نفس فحوصات الجودة محليًا في Codespaces بأمر واحد.
- ملفات حزم التحديث ونسخ APK المحلية محمية من الإضافة العرضية إلى Git عبر `.gitignore`.
